import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#69a4ff",
    },
    secondary: {
      main: "#252b1f",
    },
  },
});

export default theme;
